# APK 解析、回包、重签名流程

本文记录 `dx-apk` 目录下 APK 的解析、重新打包、zipalign 对齐和 apksigner 重签名流程。

以下路径均以项目根目录 `/Users/mac/ai/llm-ai-hooks` 为基准。

## 当前已完成结果

- 原始 APK：`dx-apk/raw/app-channela-release.apk`
- 解析目录：`dx-apk/analysis/app-channela-release/`
- apktool framework 目录：`dx-apk/analysis/apktool-framework/`
- apktool 回包产物：`dx-apk/analysis/app-channela-release/dist/app-channela-release.apk`
- 对齐未签名包：`dx-apk/resign/app-channela-release-rebuilt-aligned.apk`
- 最终重签名 APK：`dx-apk/resign/app-channela-release-rebuilt-signed.apk`
- v4 签名 sidecar：`dx-apk/app-channela-release-rebuilt-signed.apk.idsig`
- 重签名 keystore：`dx-apk/resign/dx-apk-resign.jks`
- keystore alias：`dx-apk-resign`
- keystore/store/key 密码：`dxapk123456`

安装验证结果：`dx-apk/resign/app-channela-release-rebuilt-signed.apk` 已验证可正常安装并使用，说明当前解析、回包、对齐、重签名流程可用。

最终签名验证结果：

```text
Verifies
Verified using v2 scheme: true
Verified using v3 scheme: true
Number of signers: 1
Signer DN: CN=dx-apk-resign, OU=dev, O=dx, L=Shanghai, ST=Shanghai, C=CN
```

包信息：

```text
applicationId: com.dexin.dzapp
versionName: 3.2.16
versionCode: 18400
```

## 一键脚本

当前目录已生成 skill 脚本：

```bash
bash dx-apk/skills/apk-resign/scripts/resign_apk.sh dx-apk/raw/app-channela-release.apk
```

注意：该脚本是通用重签名脚本，默认把解析目录、对齐包、签名包输出到执行命令时所在目录。当前仓库中的已验证产物已按 `dx-apk/raw/`、`dx-apk/analysis/`、`dx-apk/resign/` 归档保存；如需严格复现当前归档目录，优先使用下面的手工命令。

脚本默认会：

1. 检查 `apktool`、`keytool`、`sdkmanager`、Android build-tools。
2. 缺少 `apktool` 时通过 Homebrew 安装。
3. 缺少 build-tools 时通过 `sdkmanager` 安装 `build-tools;36.0.0`。
4. 用工作区内 `apktool-framework/` 解析 APK，避免写入 `~/Library/apktool/framework`。
5. 使用 `apktool b` 重新打包。
6. 生成或复用 `dx-apk-resign.jks`。
7. 使用 `zipalign -p -f -v 4` 对齐。
8. 使用 `apksigner sign` 签名。
9. 使用 `apksigner verify --verbose --print-certs` 验证。

## 手工命令

如果需要手工执行，可按下面顺序：

```bash
apktool d -f \
  dx-apk/raw/app-channela-release.apk \
  -o dx-apk/analysis/app-channela-release \
  -p dx-apk/analysis/apktool-framework

apktool b \
  dx-apk/analysis/app-channela-release \
  -p dx-apk/analysis/apktool-framework

keytool -genkeypair -v \
  -keystore dx-apk/resign/dx-apk-resign.jks \
  -storepass dxapk123456 \
  -keypass dxapk123456 \
  -alias dx-apk-resign \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000 \
  -dname "CN=dx-apk-resign, OU=dev, O=dx, L=Shanghai, ST=Shanghai, C=CN"
/opt/homebrew/share/android-commandlinetools/build-tools/36.0.0/zipalign \
  -p -f -v 4 \
  dx-apk/analysis/app-channela-release/dist/app-channela-release.apk \
  dx-apk/resign/app-channela-release-rebuilt-aligned.apk

/opt/homebrew/share/android-commandlinetools/build-tools/36.0.0/apksigner sign \
  --ks dx-apk/resign/dx-apk-resign.jks \
  --ks-key-alias dx-apk-resign \
  --ks-pass pass:dxapk123456 \
  --key-pass pass:dxapk123456 \
  --out dx-apk/resign/app-channela-release-rebuilt-signed.apk \
  dx-apk/resign/app-channela-release-rebuilt-aligned.apk

/opt/homebrew/share/android-commandlinetools/build-tools/36.0.0/apksigner verify \
  --verbose --print-certs \
  dx-apk/resign/app-channela-release-rebuilt-signed.apk
```

## 注意事项

- 重签名证书与原 APK 官方证书不同，不能覆盖安装原官方签名包；设备上如已安装原包，需要先卸载。
- 如果要覆盖安装，必须使用原始签名证书重新签名。
- `targetSdkVersion=36` 的 APK 不建议只用 `jarsigner`，应使用 `apksigner` 的 APK Signature Scheme v2/v3。
- Android command line tools 默认安装在 `/opt/homebrew/share/android-commandlinetools`。
- `sdkmanager` 需要 JDK 17+；本机可使用 `/opt/homebrew/opt/openjdk/libexec/openjdk.jdk/Contents/Home`。
